def cm_to_steps(distance, distance_per_step):
    steps = distance / distance_per_step
    
    return steps

def steps_to_cm(steps, distance_per_step):
	distance = steps * distance_per_step
      
	return distance